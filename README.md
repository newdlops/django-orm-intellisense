# Django ORM Intellisense

VS Code extension scaffold for building Django ORM developer tooling.

## Development

```bash
npm install
npm run compile
```

Then press `F5` in VS Code to launch an Extension Development Host.

## Available command

- `Django ORM Intellisense: Show Status`
